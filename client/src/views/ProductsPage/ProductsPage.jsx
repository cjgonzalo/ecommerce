import React from 'react'

const productsPage = ({type}) => {

    // hacer fetch a /api/"type" y mostrar los productos de ese tipo

    return (
        <div>
            <h1>{type} test view</h1>
        </div>
    )
}

export default productsPage
